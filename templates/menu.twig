{% if menu %}
    <ul>
        {% for item in items %}
            <li class="{{ item.classes | join(' ') }}">
                <a target="{{ item.target }}" href="{{ item.link }}">{{ item.title }}</a>
                {# Check if there are child items under the menu item.#}
                {% if item.children is not empty %}
                    {% include "menu.twig" with {'items': item.children} %}
                {% endif %}
            </li>
        {% endfor %}
    </ul>
{% endif %}
